# Domain docs

This is a single-context repository. Before engineering work, read root `CONTEXT.md` and relevant ADRs in `docs/adr/`.

Use the glossary's canonical vocabulary in plans, issues, tests, and implementation notes. Surface any conflict with an ADR instead of silently changing the decision.
